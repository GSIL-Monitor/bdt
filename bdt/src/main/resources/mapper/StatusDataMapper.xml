<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.com.infaith.module.mapper.StatusDataMapper">
  <resultMap id="BaseResultMap" type="cn.com.infaith.module.model.StatusData">
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="table_no" jdbcType="INTEGER" property="tableNo" />
    <result column="battle_no" jdbcType="INTEGER" property="battleNo" />
    <result column="fit_no" jdbcType="INTEGER" property="fitNo" />
    <result column="status" jdbcType="INTEGER" property="status" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from status_data
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="cn.com.infaith.module.model.StatusData">
    insert into status_data (table_no, battle_no,
      fit_no, status, is_delete, update_time)
    values (#{tableNo,jdbcType=INTEGER}, #{battleNo,jdbcType=INTEGER},
      #{fitNo,jdbcType=INTEGER}, #{status,jdbcType=INTEGER}, 0, now())
  </insert>

  <insert id="addStatusDataList" parameterType="cn.com.infaith.module.model.StatusData">
    insert into status_data (table_no, battle_no,
      fit_no, status, is_delete)
    values
    <foreach collection="list" item="item" separator=",">
      (#{item.tableNo,jdbcType=INTEGER}, #{item.battleNo,jdbcType=INTEGER},
      #{item.fitNo,jdbcType=INTEGER}, #{item.status,jdbcType=INTEGER}, 0)
    </foreach>
  </insert>

  <update id="updateByPrimaryKey" parameterType="cn.com.infaith.module.model.StatusData">
    update status_data
    set
      battle_no = #{battleNo,jdbcType=INTEGER},
      fit_no = #{fitNo,jdbcType=INTEGER},
      status = #{status,jdbcType=INTEGER},
      update_time = now()
    where table_no = #{tableNo}
  </update>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select id, table_no, battle_no, fit_no, status
    from status_data
    where id = #{id,jdbcType=INTEGER} AND is_delete = 0
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, table_no, battle_no, fit_no, status, update_time
    from status_data WHERE is_delete = 0 ORDER BY table_no
  </select>

  <select id="getStatusByTableNo" resultMap="BaseResultMap" parameterType="int">
    select id, table_no, battle_no, fit_no, status
    from status_data
    WHERE table_no = #{tableNo} AND is_delete = 0
    ORDER BY battle_no DESC, fit_no DESC
    limit 1
  </select>

  <update id="updateStatusByTableNo">
    update status_data
    set status = #{status}
    where table_no = #{tableNo} AND is_delete = 0
    AND battle_no = #{battleNo} AND fit_no = #{fitNo}
  </update>
</mapper>